# Week 3: dataset adapters, the model client layer and the planner.
#
# Installed separately from the base requirements so that a failure here can
# never break the perception and control modules, which must keep working with
# none of these packages present.
datasets>=3.0
langchain>=0.3
langchain-openai>=0.2
